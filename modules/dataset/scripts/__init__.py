"""Dataset build scripts for corpus sampling and classifier evaluation.

These CLI entry points live under ``modules/dataset/scripts`` and write
processed JSONL corpora plus evaluation tables under ``modules/dataset/results``.
"""
